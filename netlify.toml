[build]
  command = "npm run build"
  publish = "dist"

# Rule cho Admin App (Ưu tiên trước)
[[redirects]]
  from = "/admin/*"
  to = "/admin/index.html"
  status = 200

# Rule cho Landing Page (Catch-all)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200